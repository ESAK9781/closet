# Paperwork is personal information. The ONLY files from dump/ that may enter git are the two
# blank templates. Everything else in dump/ -- and any PDF anywhere else -- stays local.
/dump/*
*.pdf
!/dump/F10_nuked_final.pdf
!/dump/F174_nuked_final.pdf

# Parsed paperwork, page images and edits (personal information)
/metadata/

# Export of the official discipline tracker (personal information)
/CS07 Discipline Tracker_google_sheet_format/

# Local install
/.venv/
__pycache__/
*.pyc
